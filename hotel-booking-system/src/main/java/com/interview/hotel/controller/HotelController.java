package com.interview.hotel.controller;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class HotelController {
    // TODO: Implement endpoints:
    // POST   /api/rooms                                        - Add a room
    // GET    /api/rooms/available                              - Search available rooms (checkIn, checkOut, roomType, guests)
    // POST   /api/reservations                                 - Make a reservation
    // DELETE /api/reservations/{reservationId}                 - Cancel reservation
    // POST   /api/reservations/{reservationId}/check-in        - Check in
    // POST   /api/reservations/{reservationId}/check-out       - Check out
    // GET    /api/guests/{guestId}/reservations                - Guest reservation history
}
